const BASE_URL = process.env.NEXT_PUBLIC_ENDPOINTS;
const BLOGS_URL = `${BASE_URL}/api/blogs`;
const EXPERTS_URL = `${BASE_URL}/api/experts`;

const API_BLOGS = "/api/blogs";

export { BLOGS_URL, EXPERTS_URL, API_BLOGS };
