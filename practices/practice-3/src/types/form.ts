export interface FormProps {
  categoryId: {
    value: string,
    rules: string[],
    error: string,
  },
  name: {
    value: string,
    rules: string[],
    error: string,
  },
  price: {
    value: string,
    rules: string[],
    error: string,
  },
  quantity: {
    value: string,
    rules: string[],
    error: string,
  },
  description: {
    value: string,
    rules: string[],
    error: string,
  },
  images: {
    value: string,
    rules: string[],
    error: string,
  }
}